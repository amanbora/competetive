#include<stdio.h>
int main(){
  int t;
  scanf("%d",&t);
  while(t--){
    int arr[10000];
    int n,k;
    scanf("%d",n);
    scanf("%d",k);
    for(int i=0;i<n;i++)
       scanf("%d",&arr[i]);
      
  }
}
